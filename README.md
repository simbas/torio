Torio
---

```shell
git clone --recurse-submodules git@github.com:simbas/torio.git ~/.torio
echo "export TORIO_HOME=~/.torio" >> ~/.zshrc
echo "export PUTIO_TOKEN=xxx" >> ~/.zshrc
ln -sfnv ~/.torio/torio.sh /usr/local/bin/torio
```
